# Legacy / optional copy

**Deploy uses `wpf-browser/website/`** (see `update-vps.ps1`).

This folder is not uploaded to the VPS. Keep it in sync with `wpf-browser/website/` if you use it locally, or ignore it.
